<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leitor_qr_code">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Leitor_Qr_Code">

        <!-- Tela de LoginCadastro (inicial) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de Cadastro -->
        <activity
            android:name=".CadastroActivity"
            android:exported="true" />

        <!-- Tela de Dicas -->
        <activity
            android:name=".dicas.DicasActivity"
            android:exported="true" />

        <!-- Tela Principal / Home (contém BottomNavigationView) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Tela de Detalhes do Evento -->
        <activity
            android:name=".ui.organizador.DetalhesEventoActivity"
            android:exported="true" />

    </application>

</manifest>
